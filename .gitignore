# 1. 보안 파일 (절대 올리면 안 됨!)
.env
secrets.json

# 2. 가상환경 폴더
venv/
env/

# 3. 파이썬 컴파일 파일 (불필요)
__pycache__/
*.pyc

# 4. 데이터베이스 (로컬 테스트용 DB는 충돌 방지를 위해 제외)
db.sqlite3

# 5. OS 및 에디터 설정 파일
.DS_Store
.vscode/
.idea/